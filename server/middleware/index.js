const handleError = require("./handleError");
const handleAuth = require("./handleAuth");
const handleAdmin = require("./handleAdmin");
const handleMulter = require("./handleMulter");
const handleValidObjectId = require("./handleValidObjectId");

module.exports = {
  handleError,
  handleAuth,
  handleAdmin,
  handleMulter,
  handleValidObjectId,
};
